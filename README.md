This is a fresh project made without any boilerplate as a template using Framer motion and pure CSS.

to run the project in your terminal run the following command
npm install
and once installed run npm run dev
